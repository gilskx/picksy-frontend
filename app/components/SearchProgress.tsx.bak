"use client";

export default function SearchProgress({ query }: any) {

  return (
    <div style={{
      maxWidth: "900px",
      margin: "30px auto",
      background: "rgba(15,23,42,0.9)",
      borderRadius: "16px",
      padding: "30px",
      textAlign: "center",
      border: "1px solid rgba(255,255,255,0.1)"
    }}>

      {/* TITLE */}
      <div style={{
        color: "#93c5fd",
        fontSize: "14px",
        marginBottom: "20px"
      }}>
        ✨ Searching for "{query}"...
      </div>

      {/* ORBIT AREA */}
      <div style={{
        position: "relative",
        height: "120px",
        marginBottom: "20px"
      }}>

        {/* CENTER ICON */}
        <div style={{
          width: "70px",
          height: "70px",
          borderRadius: "50%",
          margin: "0 auto",
          background: "radial-gradient(circle, #3b82f6, #6366f1)",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          color: "#fff",
          fontSize: "22px",
          boxShadow: "0 0 30px rgba(59,130,246,0.6)"
        }}>
          🔍
        </div>

        {/* ORBIT ICONS */}
        <div className="orbit amazon">a</div>
        <div className="orbit walmart">🟡</div>
        <div className="orbit ebay">e</div>

      </div>

      {/* PROGRESS TEXT */}
      <div style={{
        color: "#cbd5f5",
        fontSize: "14px",
        marginBottom: "10px"
      }}>
        Searching products...
      </div>

      {/* PROGRESS BAR */}
      <div style={{
        height: "6px",
        borderRadius: "10px",
        background: "rgba(255,255,255,0.1)",
        overflow: "hidden"
      }}>
        <div style={{
          width: "60%",
          height: "100%",
          background: "linear-gradient(90deg, #3b82f6, #6366f1)",
          animation: "progressMove 2s infinite linear"
        }} />
      </div>

    </div>
  );
}